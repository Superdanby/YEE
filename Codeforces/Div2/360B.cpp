#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    string inp;
    cin>>inp;
    cout<<inp;
    reverse(inp.begin(),inp.end());
    cout<<inp<<"\n";
    return 0;
}
